{
  "name": "test-2",
  "version": "1.0.0",
  "description": "In this folder you should find a `data.js` file. The file exports 3 arrays: `investments`, `companies` and `users`.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mongodb": "^3.6.7"
  }
}
